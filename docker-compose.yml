services:
  db:
    image: "pgvector/pgvector:pg17"
    environment:
      POSTGRES_USER: docusense
      POSTGRES_PASSWORD: docusense
      POSTGRESS_DB: docusense
    
    ports:
      - "5432:5432"
    
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: